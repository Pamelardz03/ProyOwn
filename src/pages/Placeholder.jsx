export default function Placeholder({ title }) {
  return (
    <div className="card">
      <h2>{title}</h2>
      <p>Próximamente — esta sección se construye en una fase posterior del roadmap.</p>
    </div>
  )
}
